# Abbreviations (Fish's preferred alternative to aliases)
# Managed by chezmoi

# Git
abbr -a g git
abbr -a ga 'git add'
abbr -a gc 'git commit'
abbr -a gp 'git push'
abbr -a gl 'git pull'
abbr -a gd 'git diff'
abbr -a gs 'git status'

# chezmoi
abbr -a cm chezmoi
abbr -a cma 'chezmoi apply'
abbr -a cme 'chezmoi edit'
abbr -a cmd 'chezmoi diff'

# uv
abbr -a uvs 'uv sync --extra dev'
